<app-label key={{@key}} type={{@type}} curved={{@curved}} closeable={{if @onClose true}} ...attributes>

	{{~@text~}}

	{{~yield~}}

	{{~#if @onClose~}}
		<close role="button" {{on "click" @onClose}}>✕</close>
	{{~/if~}}

</app-label>