<option title={{@title}} label={{@label}} value={{@value}} disabled={{@disabled}} selected={{eq @value @selected}} ...attributes>
	{{#if hasBlock}}
		{{~yield~}}
	{{else}}
		{{@label}}
	{{/if}}
</option>