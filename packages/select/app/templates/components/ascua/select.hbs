<select {{on "change" this.changed}} {{did-insert this.didCreate}} multiple={{@multiple}} ...attributes>
	{{yield (hash
		group=(component "ascua/select/group" selected=@value)
		option=(component "ascua/select/option" selected=@value)
	)}}
</select>