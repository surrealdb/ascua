<select {{on "change" this.changed}} {{did-insert this.didInsert}} multiple={{@multiple}} ...attributes>
	{{yield (hash
		group=(component "ascua/select/group")
		option=(component "ascua/select/option")
	)}}
</select>