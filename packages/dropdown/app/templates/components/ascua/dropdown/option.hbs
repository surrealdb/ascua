<app-dropdown-option {{on "click" this.clicked}} {{did-insert this.didCreate}} {{will-destroy this.willDelete}} disabled={{@disabled}} selected={{this.selected}} ...attributes>
	{{#if hasBlock}}
		{{~yield~}}
	{{else}}
		{{@label}}
	{{/if}}
</app-dropdown-option>