<app-dropdown-option
	disabled={{@disabled}}
	filtered={{matches @label @dropdown.search}}
	selected={{includes (await @value) @dropdown.value}}
	{{on "click" (fn @dropdown.changed @value)}}
	...attributes>
	{{@label}}
</app-dropdown-option>