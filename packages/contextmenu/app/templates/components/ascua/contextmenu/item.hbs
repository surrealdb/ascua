<context-menu-item
	type={{this.type}}
	enabled={{this.enabled}}
	visible={{this.visible}}
	checked={{this.checked}}
	{{on "click" this.didClick}}
	{{on "mouseup" this.didMouse}}
	{{did-insert this.didCreate}}
	{{will-destroy this.willDelete}}
>

	{{@label}}

</context-menu-item>