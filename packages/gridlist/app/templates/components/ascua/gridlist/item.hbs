<app-gridlist-item
	list={{eq "l" @conf.k}}
	grid={{eq "g" @conf.k}}
	selected={{@selected}}
	{{did-insert this.didCreate}}
	{{did-update this.didChange
		@total
		@conf.a
		@conf.e.w
		@conf.v.c
		@conf.i.w
		@conf.i.h
		@item.index
	}}
	{{on "click" this.didClick}}
	{{on "dblclick" this.dblClick}}
	{{on "contextmenu" this.ctxClick}}
	...attributes
>
	{{~yield~}}
</app-gridlist-item>