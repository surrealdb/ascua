<h1>moment.js helpers</h1>

<h2>moment</h2>

<Example @name="demo-libraries-momentjs-moment.hbs">
	{{moment (now)}}
</Example>

<h2>moment-add</h2>

<p>See <a href="https://momentjs.com/docs/#/manipulating/add/" rel="noopener noreferrer" target="_blank">Moment.js add</a> for more information.</p>

<Example @name="demo-libraries-momentjs-moment-add.hbs">
	{{moment-add (now) 2 "days"}}
</Example>

<Example @name="demo-libraries-momentjs-moment-add-options.hbs">
	{{moment-add (now) days=2 months=1}}
</Example>

<h2>moment-calendar</h2>

<p>See <a href="https://momentjs.com/docs/#/displaying/calendar-time/" rel="noopener noreferrer" target="_blank">Moment.js calendar time</a> for more information.</p>

<Example @name="demo-libraries-momentjs-moment-calendar.hbs">
	{{moment-calendar (now)}}
</Example>

<Example @name="demo-libraries-momentjs-moment-calendar-reference.hbs">
	{{moment-calendar (moment-add (now) 2 "days")}}
</Example>

<Example @name="demo-libraries-momentjs-moment-calendar-format.hbs">
	{{moment-calendar (now) format=(hash sameDay="[Today]")}}
</Example>

<h2>moment-diff</h2>

<p>See <a href="https://momentjs.com/docs/#/displaying/difference/" rel="noopener noreferrer" target="_blank">Moment.js difference</a> for more information.</p>

<Example @name="demo-libraries-momentjs-moment-diff.hbs">
	{{moment-diff (now) "2016-06-21"}} milliseconds
</Example>

<Example @name="demo-libraries-momentjs-moment-diff-precision.hbs">
	{{moment-diff (now) "2016-06-21" precision="years"}} years
</Example>

<Example @name="demo-libraries-momentjs-moment-diff-fraction.hbs">
	{{moment-diff (now) "2016-06-21" precision="years" fraction=true}} years
</Example>

<h2>moment-format</h2>

<p>See <a href="https://momentjs.com/docs/#/displaying/format/" rel="noopener noreferrer" target="_blank">Moment.js format</a> for more information.</p>

<Example @name="demo-libraries-momentjs-moment-format.hbs">
	{{moment-format (now)}}
</Example>

<Example @name="demo-libraries-momentjs-moment-format-simple.hbs">
	{{moment-format (now) "ddd, hA"}}
</Example>

<Example @name="demo-libraries-momentjs-moment-format-complex.hbs">
	{{moment-format (now) "dddd, MMMM Do YYYY, h:mm:ss a"}}
</Example>

<h2>moment-relative</h2>

<Example @name="demo-libraries-momentjs-moment-relative.hbs">
	{{moment-relative (now)}}
</Example>

<Example @name="demo-libraries-momentjs-moment-relative-reference.hbs">
	{{moment-relative "2016-06-21" (now)}}
</Example>

<Example @name="demo-libraries-momentjs-moment-relative-suffix.hbs">
	{{moment-relative "2016-06-21" (now) ignoreSuffix=true}}
</Example>

<h2>moment-sub</h2>

<p>See <a href="https://momentjs.com/docs/#/manipulating/subtract/" rel="noopener noreferrer" target="_blank">Moment.js subtract</a> for more information.</p>

<Example @name="demo-libraries-momentjs-moment-sub.hbs">
	{{moment-sub (now) 2 "days"}}
</Example>

<Example @name="demo-libraries-momentjs-moment-sub-options.hbs">
	{{moment-sub (now) days=2 months=1}}
</Example>

<h2>now</h2>

<Example @name="demo-libraries-momentjs-now.hbs">
	{{now}}
</Example>

<h2>utc</h2>

<Example @name="demo-libraries-momentjs-utc.hbs">
	{{utc}}
</Example>

<Example @name="demo-libraries-momentjs-utc-now.hbs">
	{{utc (now)}}
</Example>